# Docker Compose 專案名稱，類似命名空間統整所有資源，用作容器和網路前綴，同一個專案名稱下的服務可以互相通訊
#COMPOSE_PROJECT_NAME=docker_workspace

# 掛載路徑基準：預設為 compose.yaml 所在目錄（repo 根目錄），跨專案共用設定時可改為絕對路徑
PROJECT_ROOT=.

# 容器內 mars 帳號的 UID/GID
# macOS（Docker Desktop）維持 1000 即可，virtiofs 會自動處理權限映射
# Linux 宿主機請改成 `id -u` / `id -g` 的結果，避免 bind mount 權限問題
UID=1000
GID=1000

# code-server 登入密碼（http://localhost:8443）
CODE_SERVER_PASSWORD=change-me

# Traefik + Let's Encrypt（DNS-01, Cloudflare）：憑證涵蓋 BASE_DOMAIN 與 *.BASE_DOMAIN
# 根網域：實際存取用 <service>.BASE_DOMAIN，萬用字元憑證也以此簽發
BASE_DOMAIN=example.com
# 通知信箱（憑證異常/到期會寄信到這）
ACME_EMAIL=you@example.com
# Cloudflare API Token：該 zone 需有 Zone:Zone:Read + Zone:DNS:Edit 權限（前者查 zone ID、
# 後者建 TXT 驗證紀錄，缺一會 403），在 Cloudflare dashboard 建立
CF_DNS_API_TOKEN=

# Playwright Browser Host（tools/playwright/）的對外監聽 port，選配、預設 3910
# 不需要跟其他機器上的類似服務共用同一台 Mac 時不用改
#PLAYWRIGHT_HOST_PORT=3910
